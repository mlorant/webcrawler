from peewee import SqliteDatabase

DATABASE = SqliteDatabase(threadlocals=True)

START_URLS = ('http://www.aau.dk',
              'http://www.python.org')

MAX_THREADS = 1

TIMEOUT = 5.0  # in seconds

ROBOTS_TXT_CACHE = 60 * 5

USER_AGENT = 'LeFatCrawler'

MAX_PAGES_TO_CRAWL = 1000

LOGFILE = 'logs/crawl.log'
